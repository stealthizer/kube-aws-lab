apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress
spec:
  type: NodePort
  ports:
    - port: 1080
      nodePort: 30000
      name: http
    - port: 18080
      nodePort: 32767
      name: http-mgmt
  selector:
    app: nginx-ingress-lb
